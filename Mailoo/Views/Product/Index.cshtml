@model IEnumerable<Product>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Items View</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="New" class="btn btn-primary">
                <i class="bi bi-plus"></i> &nbsp; Add New Item
            </a>
        </div>
    </div>

    @* <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">
                    Item Name
                </th>
                <th scope="col">
                    Item Price
                </th>
                <th scope="col">
                    Description
                </th>
                <th scope="col">
                    Img
                </th>
            </tr>
        </thead>
        <tbody> *@
            <div class="container">
                <div class="row justify-content-center">
                    @foreach (var item in Model)
                    {
                        <div class="col-md-4 col-sm-6 d-flex justify-content-center">
                            <div class="card" style="width: 18rem;">
                                <img src="@item.imageSrc" class="card-img-top" alt="Product Image">
                                <div class="card-body">
                                    <h5 class="card-title">@item.Name</h5>
                                    <p class="card-text">@item.Description</p>
                                    <p>Price @item.Price</p>
                            <a asp-controller="Product" asp-action="Edit" asp-route-id="@item.ID" class="btn btn-primary btn-sm btn-success">
                                <i class="bi bi-trash3"></i> Edit
                            </a>
                           
                                </div>
                            </div>
                        </div>
                
                    }
                </div>
            </div>
   

           @*  @foreach (var item in Model)
            {
                <tr class="table-primary">
                    <td scope="row" width="30%">
                        @item.Name
                    </td>
                    <td width="20%">
                        @item.Price
                    </td>
                    <td width="20%">
                        @item.Description
                    </td>
                    <td>
                        <img src="@item.imageSrc" style="width:48px" class="img-thumbnail"/>
                    </td>
                    <td>
                        <a asp-controller="Product" asp-action="Edit" asp-route-Id="@item.ID" class="btn btn-sm btn-success">
                            <i class="bi bi-pencil"></i>
                        </a>
                        &nbsp;
                        <a asp-controller="Product" asp-action="Delete" asp-route-Id="@item.ID" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash3"></i>
                        </a>
                    </td>
                </tr>
            } *@
      @*   </tbody>
    </table> *@
</div>